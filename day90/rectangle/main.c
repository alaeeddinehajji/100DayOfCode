#include <stdlib.h>
int main() {
    
    // This is just a simple project #100dayofcode
    //
    // My twitter Account : @AlaeeddineHajji
    
    int l = 15, p = 15, s = 15, d = 15;

    for (p=1; p<=s; p++)
    {
    printf ("\n");
    for (l=1;l<=d; l++)

    {

    if(p==1 || l==1 || l==d || s==p 
    || p==s/2 || l==l/2)

    printf("*"); else printf(" ");


    }
  }
      return 0;
    }
