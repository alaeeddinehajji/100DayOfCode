#include <stdlib.h>
int main() {
    int l=50;
    int p=58;
    int s=50;
    int d=50;

    for (p=1; p<=s; p++)
    {
    printf ("\n");
    for (l=1;l<=d; l++)

    {

    if(p==1 || l==1 || l==d || s==p 
    || p==s/2 || l==l/2)

    printf("*"); else printf(" ");


    }
  }
      return 0;
    }
